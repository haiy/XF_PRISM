测试目的：
    测试XF选择算法对不同数据类型的有效性，当然还有对不同相互关系的有效性。具体的测试方法就是，测试其是否能够从模拟的数据集合中找到特定的特征组合。比如，最后的类特征是由前5个特征之和组成的。

测试思路
1.生成范围在（0，500）内的100个点，分别满足下面的6个方程，
然后分别对每个方程的每个测试类别生成四组数据。每组数据的前
6列依次是对应的方程的x值，此值为随机数。后面5列为也为完全随机数，但是最后一列的y值为前6列的随机数作为自变量经过方程
处理后的和，也就是说，y=y1(x1)+y2(x2)+...+y5(x5)+y6(x6)

2.方程顺序为：
func_list=[linear_fun,non_linear,fourior_fun,sin_fun,cube_fun,norm_fun]

0:  y=x
1:  y=x*sin(x)
2:  y=sin(16*PI*x)+x
3:  y=sin(x)
4:  y=x^3
5:  y=正态函数
